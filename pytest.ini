[pytest]
testpaths = tests
addopts = -q
markers =
    matlab: requires a MATLAB engine + QuaDRiGa (set PPO_MATLAB_TESTS=1)
filterwarnings =
    ignore::DeprecationWarning:train_ppo
    ignore::DeprecationWarning:ppo_experiment
